{
  "name": "speech-to-text-telegram",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Self-deployable Telegram voice-to-text bot on Cloudflare Workers using the free Gemini API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
